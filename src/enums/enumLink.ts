export enum LINK {
  LOGIN = '/login',
  AUTH = '/auth',
  PROFILE = '/profile',
  RECOVER = '/recovery',
  PASSWORD = '/password',
  FOUND404 = '/notfound',
  REGISTER = '/register',
  TEST = '/test',
}
